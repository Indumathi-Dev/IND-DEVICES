{
  "name": "agent-server",
  "version": "1.0.0",
  "private": true,
  "description": "IND-DEVICES agent server — BFF that holds the Anthropic API key server-side and exposes WCAG 2.2 AI audit endpoints",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "axios": "^1.18.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
